{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://weave.dev/contracts/checkpoint-manifest-v1.schema.json",
  "$defs": {
    "sha256": {
      "type": "string",
      "pattern": "^[a-f0-9]{64}$"
    },
    "generation": {
      "type": "string",
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._-]{0,63}$"
    },
    "delta_path": {
      "type": "string",
      "pattern": "^deltas/([A-Za-z0-9][A-Za-z0-9._-]{0,127}/)*[A-Za-z0-9][A-Za-z0-9._-]{0,127}\\.sql$"
    },
    "archive_path": {
      "type": "string",
      "pattern": "^legacy-deltas/v1/([A-Za-z0-9][A-Za-z0-9._-]{0,127}/)*[A-Za-z0-9][A-Za-z0-9._-]{0,127}\\.sql$"
    },
    "covered_delta": {
      "type": "object",
      "additionalProperties": false,
      "required": ["raw_sha256", "original_path", "archived_path", "disposition"],
      "properties": {
        "raw_sha256": {
          "$ref": "#/$defs/sha256"
        },
        "original_path": {
          "$ref": "#/$defs/delta_path"
        },
        "archived_path": {
          "$ref": "#/$defs/archive_path"
        },
        "disposition": {
          "const": "incorporated"
        }
      }
    }
  },
  "type": "object",
  "additionalProperties": false,
  "required": ["format", "generation", "state_sha256", "incorporated_legacy_deltas"],
  "properties": {
    "format": {
      "const": "weave.checkpoint.v1"
    },
    "generation": {
      "$ref": "#/$defs/generation"
    },
    "state_sha256": {
      "$ref": "#/$defs/sha256"
    },
    "incorporated_legacy_deltas": {
      "type": "object",
      "additionalProperties": {
        "$ref": "#/$defs/covered_delta"
      }
    }
  }
}
